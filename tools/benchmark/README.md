# tools/benchmark

Benchmark harnesses.

Measured results live in `docs/implementation/BENCHMARKS.md`. Figures gathered so far were
taken directly from stack operations rather than from a dedicated harness; a repeatable
harness belongs here once a full-length work has been analysed end to end.
