# tools/admin

Administrative utilities.

Day-to-day operations are covered by `infra/scripts/mp2.sh` (start/stop, migrate, backup,
restore, object inspection, workflow inspection, local-only and port checks). Add tools here
only when they are genuinely separate from that entrypoint, so the documented path and the
real path do not drift.
